// Совместимый шим: [BookMetadataDisplay], [formatGrouped] и
// [bookInlinesToPlainText] живут в re_ucm_core (name_utils).
// Новым кодом импортировать их оттуда напрямую.
export 'package:re_ucm_core/utils/name_utils.dart' show
    BookMetadataDisplay,
    formatGrouped,
    bookInlinesToPlainText;
